async function maybeUpdateProfile(userId, conversationId) {
    // placeholder — does nothing yet
    console.log("maybeUpdateProfile called for user:", userId, "conversation:", conversationId);
}

module.exports = { maybeUpdateProfile };